pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
             bat 'mvn --version'
                bat 'mvn -B -DskipTests clean package'
                }
            }

        stage('Test') {
            steps {
                junit 'target/surefire-reports/**/*.xml'
            }
        }
 }   }